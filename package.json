{
  "name": "olas-mech-marketplace",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "generate-manifests": "node scripts/generate-manifests.js",
    "deploy-gnosis": "graph deploy --network gnosis --studio olas-gnosis-mech-marketplace subgraph.gnosis.yaml",
    "deploy-base": "graph deploy --network base --studio olas-base-mech-marketplace subgraph.base.yaml",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.82.0",
    "@graphprotocol/graph-ts": "0.32.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
