apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.name }}-db-claim
spec:
  storageClassName: {{ .Values.postgres.storageClass }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
