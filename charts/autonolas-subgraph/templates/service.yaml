apiVersion: v1
kind: Service
metadata:
  name: {{ include "autonolas-subgraph.fullname" . }}
  labels:
    {{- include "autonolas-subgraph.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autonolas-subgraph.selectorLabels" . | nindent 4 }}
