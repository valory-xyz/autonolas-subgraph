apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "autonolas-subgraph.fullname" . }}-test-connection"
  labels:
    {{- include "autonolas-subgraph.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "autonolas-subgraph.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
