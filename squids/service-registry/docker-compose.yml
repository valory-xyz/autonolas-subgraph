services:
  db:
    container_name: service-registry-squid-db
    image: postgres:16
    environment:
      POSTGRES_DB: squid
      POSTGRES_PASSWORD: postgres
    ports:
      - "23800:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
