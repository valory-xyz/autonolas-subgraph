services:
  db:
    container_name: marketplace-squid-db
    image: postgres:16
    environment:
      POSTGRES_DB: squid
      POSTGRES_PASSWORD: postgres
    ports:
      - "23801:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
