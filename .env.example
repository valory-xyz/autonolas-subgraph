# Subgraph Deployment Configuration

# Basic authentication credentials for Graph Node access
# These will be used with the predefined URLs for staging and production environments
BASIC_AUTH_USER=your-username
BASIC_AUTH_PASSWORD=your-password

# IPFS Registry URL (optional - defaults to https://registry.autonolas.tech if not set)
IPFS_REGISTRY=https://registry.autonolas.tech