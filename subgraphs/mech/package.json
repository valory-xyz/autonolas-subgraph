{
  "name": "mech",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ mech",
    "create-local": "graph create --node http://localhost:8020/ mech",
    "remove-local": "graph remove --node http://localhost:8020/ mech",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mech",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.98.1",
    "@graphprotocol/graph-ts": "0.38.2",
    "node-fetch": "3.3.1"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  },
  "resolutions": {
    "axios": "1.18.0",
    "immutable": "5.1.8",
    "nanoid": "5.1.11",
    "tmp": "0.2.7"
  }
}
